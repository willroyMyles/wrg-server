{
	"name": "wrg-server-app",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "node index.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"heroku-postbuild": "cd wrg-autoparts && npm i && npm i --only=dev && npm run build",
		"logs": "heroku logs --tail -a wrg-server"
	},
	"author": "willroy myles",
	"license": "ISC",
	"dependencies": {
		"express": "^4.17.1"
	}
}
